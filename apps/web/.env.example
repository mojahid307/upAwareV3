# Copy this file to .env.local and fill in real values.
# The frontend runs on MOCK DATA when NEXT_PUBLIC_API_URL is empty/unset.
# Map uses OpenFreeMap (free, no API key required).

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_SOCKET_URL=
